import { PaginationReducedPipe } from './pagination-reduced.pipe';

describe('PaginationReducedPipe', () => {
  it('create an instance', () => {
    const pipe = new PaginationReducedPipe();
    expect(pipe).toBeTruthy();
  });
});

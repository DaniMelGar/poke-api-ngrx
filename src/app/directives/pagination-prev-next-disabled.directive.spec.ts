import { PaginationPrevNextDisabledDirective } from './pagination-prev-next-disabled.directive';

describe('PaginationPrevNextDisabledDirective', () => {
  it('should create an instance', () => {
    const directive = new PaginationPrevNextDisabledDirective();
    expect(directive).toBeTruthy();
  });
});

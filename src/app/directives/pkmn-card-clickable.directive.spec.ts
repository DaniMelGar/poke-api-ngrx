import { PkmnCardClickableDirective } from './pkmn-card-clickable.directive';

describe('PkmnCardClickableDirective', () => {
  it('should create an instance', () => {
    const directive = new PkmnCardClickableDirective();
    expect(directive).toBeTruthy();
  });
});

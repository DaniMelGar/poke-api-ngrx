import { ImgIconTypeErrorDirective } from './img-icon-type-error.directive';

describe('ImgIconTypeErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgIconTypeErrorDirective();
    expect(directive).toBeTruthy();
  });
});

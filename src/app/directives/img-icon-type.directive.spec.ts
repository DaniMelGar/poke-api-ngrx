import { ImgIconTypeDirective } from './img-icon-type.directive';

/*describe('ImgIconTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgIconTypeDirective();
    expect(directive).toBeTruthy();
  });
});*/

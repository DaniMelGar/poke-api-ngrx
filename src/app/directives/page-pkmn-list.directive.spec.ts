import { PagePkmnListDirective } from './page-pkmn-list.directive';

describe('PagePkmnListDirective', () => {
  it('should create an instance', () => {
    const directive = new PagePkmnListDirective();
    expect(directive).toBeTruthy();
  });
});

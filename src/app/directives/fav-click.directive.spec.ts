import { FavClickDirective } from './fav-click.directive';

describe('FavClickDirective', () => {
  it('should create an instance', () => {
    const directive = new FavClickDirective();
    expect(directive).toBeTruthy();
  });
});
